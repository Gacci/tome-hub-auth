import { ProfileImageUrlInterceptor } from './profile-image-url.interceptor';

describe('ProfileImageUrlInterceptor', () => {
  it('should be defined', () => {
    expect(new ProfileImageUrlInterceptor()).toBeDefined();
  });
});
